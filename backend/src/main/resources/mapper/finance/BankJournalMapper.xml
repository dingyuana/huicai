<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huicai.module.finance.mapper.BankJournalMapper">
    <select id="aggregateByAccountPeriod" resultType="java.util.Map">
        SELECT tx_type, SUM(amount) as total
        FROM t_bank_journal
        WHERE account_id = #{accountId}
          AND period = #{period}
          AND deleted = 0
        GROUP BY tx_type
    </select>

    <update id="updateVoucherId">
        UPDATE t_bank_journal SET voucher_id = #{voucherId} WHERE id = #{id}
    </update>

    <update id="updateReconciled">
        UPDATE t_bank_journal SET is_reconciled = #{reconciled} WHERE id = #{id}
    </update>

    <select id="sumAmountByAccount" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(CASE WHEN tx_type IN ('INCOME','TRANSFER_IN') THEN amount ELSE -amount END), 0)
        FROM t_bank_journal
        WHERE account_id = #{accountId} AND deleted = 0
    </select>

    <select id="selectUnreconciled" resultType="com.huicai.module.finance.entity.BankJournalEntity">
        SELECT * FROM t_bank_journal
        WHERE account_id = #{accountId}
          AND is_reconciled = FALSE
          AND deleted = 0
        ORDER BY tx_date
    </select>

    <update id="nullOutBusinessDocId">
        UPDATE t_bank_journal SET business_doc_id = NULL WHERE business_doc_id IS NOT NULL
    </update>
</mapper>
