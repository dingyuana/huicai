<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huicai.module.finance.mapper.CashJournalMapper">

    <select id="sumDebitByPeriod" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(debit), 0) FROM t_cash_journal
        WHERE period = #{period} AND deleted = 0
    </select>

    <select id="sumCreditByPeriod" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(credit), 0) FROM t_cash_journal
        WHERE period = #{period} AND deleted = 0
    </select>

    <select id="getLastBalance" resultType="java.math.BigDecimal">
        SELECT COALESCE(balance, 0) FROM t_cash_journal
        WHERE period = #{period} AND deleted = 0
        ORDER BY journal_date DESC, id DESC LIMIT 1
    </select>

</mapper>