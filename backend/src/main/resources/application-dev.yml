spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/huicai
    username: huicai
    password: huicai123
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

# P31: dev 环境开启发票导入后自动全流程 (审核发票→审核业务单→生凭证)
# 人仍是凭证审核的最终负责人 (凭证状态 PENDING_REVIEW 等待人工终审)
invoice:
  auto-flow-after-import: true