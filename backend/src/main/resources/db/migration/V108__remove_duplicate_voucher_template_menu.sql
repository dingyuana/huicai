-- ============================================================
-- V108: 移除重复的"凭证模板"菜单项
--
-- 背景：V96 菜单重构后，凭证模板在"基础数据→凭证类型"的 tab 内
-- 已可访问，同时在"财务核心→凭证模板" (id=32) 重复存在。
-- 本次移除菜单项 #32 及其角色关联。
-- ============================================================

-- 1. 清除角色-菜单关联
DELETE FROM t_role_menu WHERE menu_id = 32;

-- 2. 逻辑删除菜单项
UPDATE t_menu SET deleted = 1 WHERE id = 32;