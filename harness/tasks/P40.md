# P40 任务：AI 服务骨架增强

## 任务描述
完善现有 ai-service 骨架，添加 FastAPI 健康检查、Swagger 文档、CORS 配置，Docker 化就绪

## 任务优先级
高

## 预计工时
0.5 人日

## 关联 SPEC
文档路径：`docs/specs/P40-ai-service-skeleton-enhancement.md`

## 必须遵循的规范
1. `harness/ARCHITECTURE.md` - 三层 Loop 架构
2. `docs/coding-conduct.md` - 编码规范
3. `docs/layer-architecture.md` - 分层架构

## 具体要求

### 1. 保持现有结构不变
保留所有现有文件，只做增强

### 2. 新增文件
| 文件 | 内容 |
|------|------|
| `ai-service/app/core/config.py` | Pydantic Settings 配置模块 |
| `ai-service/app/core/logging.py` | 结构化日志配置 |

### 3. 修改文件
| 文件 | 修改内容 |
|------|----------|
| `ai-service/app/main.py` | 加 CORS、Swagger 配置、统一异常处理 |
| `ai-service/requirements.txt` | 确保依赖正确（pydantic-settings 等） |

### 4. API 契约
- 健康检查：`GET /ai/v1/health`
- Swagger UI：`GET /docs`
- OpenAPI Schema：`GET /openapi.json`

### 5. 验收标准
- Docker build 成功
- docker-compose up 启动成功
- 健康检查返回 ok
- Swagger UI 可访问
- CORS 配置正确

## 交付物
1. 代码文件（新增+修改）
2. 测试通过报告
3. Git commit（符合规范）
