# 基于Web财务软件的项目说明书

## 一、总体概述

项目名称：慧财财务

本方案为传统 Web 财务软件，覆盖企业财务核算全生命周期，包括基础数据、业务单据、总账、出纳、固定资产、往来、税务、预算、报表分析等核心模块。系统以 **PostgreSQL** 为核心业务数据库，利用其高级数据类型、扩展能力与强大的查询引擎，同时为 **AI 模块** 提供原生的向量存储、文本检索与任务调度支持。后端基于 Spring Boot，AI 服务独立部署，通过消息队列与主系统异步集成。系统本位币为人民币，不涉及多币种处理。

**核心目标**：

- 财务合规、数据强一致、全链路审计
- 利用 PostgreSQL 高级特性简化设计、增强性能
- AI 作为系统原生能力，实现智能填单、自动对账、异常检测、智能问答等

***

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    前端层 (Vue 3 + Element Plus)             │
│               ECharts / 报表组件 / PDF预览                   │
└─────────────────────────────────────────────────────────────┘
                              ↓ HTTP/HTTPS
┌─────────────────────────────────────────────────────────────┐
│                API 网关层 (Spring Cloud Gateway 可选)        │
│         鉴权校验 / 限流熔断 / 日志审计 / 跨域处理           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                业务应用层 (Spring Boot 3.x)                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 系统管理  │ │ 用户权限  │ │ 单据管理  │ │ 财务核心  │       │
│  │ 基础数据  │ │ RBAC    │ │ 发票/收付 │ │ 凭证/账簿  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 出纳管理  │ │ 固定资产  │ │ 往来管理  │ │ 税务管理  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│  │ 预算管理  │ │ 报表中心  │ │ 财务分析  │                    │
│  └──────────┘ └──────────┘ └──────────┘                    │
│  ┌──────────────────────────────────────────┐              │
│  │          AI 任务调度与数据服务中间件        │              │
│  └──────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────┘
              ↓                              ↓
┌─────────────────────────┐    ┌─────────────────────────────┐
│   消息队列 (RabbitMQ)   │    │   数据持久层                 │
│   AI 任务分发 / 异步处理 │    │  ┌───────────────────────┐  │
└─────────────────────────┘    │  │ PostgreSQL 16         │  │
              ↓                │  │ + pgvector / pg_trgm  │  │
┌─────────────────────────┐    │  │ + 物化视图 / 分区表   │  │
│ AI 服务层 (Python/FastAPI)│    │  └───────────────────────┘  │
│ OCR / NLP / 异常检测     │    │  ┌────────┐ ┌──────────┐    │
│ 趋势预测 / 智能问答      │    │  │ Redis  │ │ MinIO    │    │
└─────────────────────────┘    │  │ (缓存) │ │(文件存储) │    │
                               │  └────────┘ └──────────┘    │
                               └─────────────────────────────┘
```

***

## 三、技术选型

| 层级     | 技术栈                                            | 说明                                |
| ------ | ---------------------------------------------- | --------------------------------- |
| 后端框架   | Spring Boot 3.x + Spring Cloud (可选)            | 核心框架，初期可单体部署，未来可拆分微服务             |
| 安全框架   | Spring Security + JWT + OAuth2                 | 认证授权，支持按钮级权限                      |
| ORM    | MyBatis-Plus / JPA (Hibernate)                 | 适配 PostgreSQL，利用其高级 SQL 特性        |
| 主数据库   | **PostgreSQL 16**                              | 业务数据存储，同时承担 AI 特征数据存储             |
| 向量扩展   | **pgvector**                                   | 存储文本嵌入向量，支持相似度检索（用于对账匹配、问答）       |
| 文本搜索   | **pg\_trgm** + `tsvector`                      | 模糊匹配、全文检索（科目搜索、智能问答）              |
| 缓存 & 锁 | Redis 7                                        | 序列号生成、分布式锁、Token 黑名单、缓存           |
| 消息队列   | RabbitMQ                                       | 与 AI 服务解耦，异步任务投递                  |
| 文件存储   | MinIO                                          | 凭证影像、发票扫描件、报表导出文件                 |
| 报表引擎   | JasperReports / EasyExcel                      | 三大报表及自定义报表，支持大数据量导出               |
| AI 服务  | Python 3.11 + FastAPI + Hugging Face / PyTorch | OCR、文本分类、异常检测、预测                  |
| 前端     | Vue 3 + Element Plus + ECharts                 | 管理后台，数据可视化                        |
| 接口文档   | Knife4j (Swagger)                              | 自动生成 API 文档                       |
| 监控日志   | Prometheus + Grafana + ELK                     | 系统监控、审计日志分析                       |
| 部署     | Docker Compose / Kubernetes                    | 容器化部署，PostgreSQL 使用 Patroni 实现高可用 |

***

## 四、核心功能模块设计

### 4.1 系统管理与基础数据

| 子功能      | 说明                          |
| -------- | --------------------------- |
| 会计期间管理   | 定义会计年度及月度期间，支持自定义开始/结束日期    |
| 科目体系维护   | 支持多套会计准则模板，科目树状无限层级，可设置辅助核算 |
| 凭证类型设置   | 记账、收款、付款、转账等凭证类型，对应不同编号规则   |
| 常用摘要库    | 预置常用摘要，支持录入时快速选择，AI 可推荐摘要   |
| 小数位/精度设置 | 金额小数位数等全局参数配置               |
| 系统参数配置   | 模块开关、结账检查规则、折旧方法默认值等        |

**AI 结合点**：智能摘要推荐，根据科目和金额自动生成常用摘要文本。

### 4.2 用户与权限模块

采用 **RBAC 模型**，支持数据权限隔离。

| 子功能    | 说明                                                  |
| ------ | --------------------------------------------------- |
| 用户管理   | 用户增删改查、状态管理、关联员工                                    |
| 角色管理   | 支持多角色，角色可继承，菜单/按钮级权限控制                              |
| 数据权限   | 按部门/岗位自动过滤数据，通过 MyBatis 拦截器注入 SQL 条件                |
| 操作日志审计 | AOP + 自定义注解记录关键操作，存入 PostgreSQL 的 `jsonb` 字段中保存前后快照 |
| 登录与会话  | JWT + Redis 实现无状态会话，支持强制下线、多端登录控制                   |

### 4.3 业务单据管理

所有会计凭证的唯一数据入口，连接业务与财务。

**支持的单据类型**：

- 银行流水单（外部导入）
- 收款单、付款单
- 费用报销单
- 采购发票、销售发票
- 其他应收单、其他应付单

**银行流水导入流程**：

银行流水是系统核心数据入口之一，支持 Excel/CSV 批量导入，导入后自动进入分类流程：

```
上传对账单（Excel/CSV）
  ↓
列名智能识别（两阶段匹配：精确→子串）
  ├─ 日期列: "交易日期"/"记账日期"/"date" 等 10 种变体
  ├─ 摘要列: "摘要"/"备注"/"description"（含级联回退：摘要→交易附言→用途）
  ├─ 金额列: "收入金额"/"支出金额"/"发生金额"（含互斥降级逻辑）
  └─ 对方户名: 四层级联（付款人名称→收款人名称→对方户名→从摘要正则提取）
  ↓
自动分类引擎（两段式，100%有结果）
  ├─ 规则引擎（确定性匹配）
  │    优先级排序 → 方向过滤 → 关键词/正则匹配 → 第一命中即停
  │    8 类标准分类: bank_fee/interest_income/business_receipt/
  │                  business_payment/internal_transfer/tax_payment/
  │                  social_security/insurance_fee
  └─ 兜底启发式（硬编码关键词组，9 级分组按优先级匹配）
  ↓
导入结果 → 流水进入"待确认"状态
  ↓ 人工审查
确认/驳回/重分类
  ↓
生成业务单据 → 生成凭证
```

导入后流水状态流转：`已分类（待确认）→ 已确认 → 已生成单据 → 已生成凭证 → 已核销`。

**核心数据模型**：

- 单据主表 `t_business_doc`：单据编号、类型、日期、金额、状态、往来对象、部门、申请人、摘要、附件 ID、生成凭证 ID、OCR 数据 (`jsonb`)、文本嵌入向量 (`vector`)。
- 单据分录表 `t_business_doc_entry`：费用类别、金额、发票号、辅助核算项目。
- 附件表 `t_attachment`：通用附件，关联业务类型与 ID，实际文件存 MinIO。

**业务流程**：\
`草稿 → 提交 → 审批 → 生成凭证 → 关闭`。已生成凭证的单据不可修改，只能通过红冲调整。

**与凭证的集成**：

- 每种单据类型映射固定凭证模板（存储在 `t_voucher_template`）。
- 生成凭证时调用财务核心 `VoucherService`，同一事务内回写单据凭证 ID 并更新状态。
- 模板支持动态科目映射（如费用类别决定借方科目）。

**AI 结合点**：

- **OCR 智能填单**：上传发票/银行回单 → AI OCR 识别 → 自动填充单据字段（金额、日期、对方名称等），结果存入 `ocr_data`，人工确认后保存。
- **智能分类**：根据摘要和金额自动推荐单据类型及科目。

### 4.4 财务核心模块

#### 4.4.1 总账初始化（期初建账）

- 按末级科目录入期初余额，上级科目自动汇总。
- 试算平衡检查通过后方可“启用”，启用后锁定。
- 年结后自动生成新年度期初余额。

#### 4.4.2 科目管理

- 树形结构，编码支持多级（如 `1001.01.001`）。
- 科目属性：借贷方向、是否现金/银行科目、辅助核算类型等。
- 余额实时更新，并按期间保存快照。

#### 4.4.3 凭证管理

- **录入**：支持手工录入、模板生成、单据生成。强制借贷平衡校验。
- **状态流转**：草稿 → 已提交 → 已审核 → 已记账。已审核后可冲销（生成红字凭证），不可直接修改。
- **凭证号**：使用 Redis 原子自增生成，格式 `类型+期间+流水号`。
- **附件**：可关联原始单据影像。
- **批量处理**：支持按月批量审核、记账。

#### 4.4.4 账簿管理

- 总分类账、明细账、日记账、多栏账。
- 支持跨年查询，按科目、期间、辅助核算维度过滤。
- 与出纳模块的现金/银行日记账双向核对。

#### 4.4.5 期末处理与结账

- **期末结转向导**：严格顺序（折旧→工资→税金→成本结转→损益结转→结账）。
- **自动结转模板**：预置损益结转、成本结转模板，可一键生成凭证。
- **结账/反结账**：期间结账后所有凭证只读，反结账需最高权限并记录日志。
- **结账检查**：自动校验凭证是否全部审核记账、损益是否结转、试算平衡等。
- **年结**：年末结转余额至下年，生成年度结转凭证。

**AI 结合点**：

- **自动生成凭证**：从单据生成凭证时，AI 校验科目匹配合理性。
- **异常交易检测**：AI 服务扫描新记账凭证，标记借贷不平衡、科目串户、金额异常等风险，存入 `ai_tags`。

### 4.5 出纳管理模块

管理企业资金流动，与总账高度稽核。

| 子功能     | 说明                        |
| ------- | ------------------------- |
| 银行账户管理  | 维护企业银行账户信息，支持状态管理         |
| 银行对账单导入 | Excel/CSV 导入银行流水，自动列名识别，两段式智能分类（规则引擎+兜底启发式），导入预览，去重检测，导入日志 |
| 智能分类规则管理 | 系统管理员配置分类规则（CRUD + 排序 + 启停），8 条种子规则预置，支持 keyword/keyword_regex/counterparty_match 三种匹配类型 |
| 银行对账    | 企业账与银行账自动勾兑，手工调整          |
| 余额调节表   | 自动生成期末银行余额调节表             |
| 现金日记账   | 按日登记，日清月结，自动汇总            |
| 银行日记账   | 分账户记录流水，与银行对账单双向同步        |
| 票据管理    | 支票、汇票购入、领用、核销、库存盘点        |

**关键逻辑**：

- 日记账录入后可自动生成会计凭证。
- 日记账余额与总账科目余额强制校验，不符则阻止结账。

**AI 结合点**：

- **智能银行对账**：将企业账和银行流水摘要生成文本嵌入向量，利用 `pgvector` 计算余弦相似度，自动匹配，仅少量未命中需人工处理。
- **银行流水智能分类**：导入流水时 AI 建议对方科目并生成收款单/付款单。

### 4.6 固定资产管理模块

全生命周期管理固定资产。

| 子功能  | 说明                                   |
| ---- | ------------------------------------ |
| 资产卡片 | 编码、名称、类别、部门、原值、残值、年限、折旧方法            |
| 资产变动 | 原值调整、部门转移、状态变更，记录变动日志                |
| 折旧计提 | 按单个资产计提，支持平均年限法、双倍余额法、年数总和法，自动生成折旧凭证 |
| 资产盘点 | 生成盘点单，录入实盘数，自动计算盘盈盘亏并生成凭证            |
| 资产处置 | 报废、出售、捐赠，自动计算清理损益并生成凭证               |
| 资产报表 | 台账、折旧明细表、分类汇总表                       |

**规则约束**：当月新增下月折旧，当月减少当月照提。

### 4.7 往来管理模块

精细化管理应收应付，支持核销与坏账。

| 子功能      | 说明                               |
| -------- | -------------------------------- |
| 客户/供应商档案 | 基本信息、信用额度、账期                     |
| 往来明细账    | 按客户/供应商查询每笔业务                    |
| 账龄分析     | 按账龄区间自动分析                        |
| 核销管理     | 收款与应收发票匹配、付款与应付发票匹配，支持部分核销和多对多核销 |
| 坏账计提     | 支持账龄比例法或个别认定法，自动生成坏账准备凭证         |
| 催款提醒     | 根据信用期和账龄生成催款列表                   |

**控制**：核销后锁定，需反核销才能修改。

**AI 结合点**：智能往来核销，将发票和收付款单的摘要向量化，辅助自动核销。

### 4.8 税务管理模块

聚焦增值税管理，满足一般纳税人基本需求。

| 子功能     | 说明                              |
| ------- | ------------------------------- |
| 税种税率维护  | 增值税税率（13/9/6/0等）、附加税税率、企业所得税预缴率 |
| 进项税管理   | 进项发票登记、认证状态、进项税额汇总              |
| 销项税管理   | 销项发票登记、销项税额汇总                   |
| 应交增值税计算 | 销项-进项，自动生成结转未交增值税凭证             |
| 附加税计算   | 自动计算城建税、教育费附加等，生成凭证             |
| 纳税申报表   | 增值税申报表（基础格式）、所得税预缴表             |
| 发票台账    | 与单据模块联通，全量进销项发票记录               |

### 4.9 预算管理模块

实现事前控制，支持弹性预算。

| 子功能    | 说明                           |
| ------ | ---------------------------- |
| 预算编制   | 按部门/项目/科目编制月度、季度、年度预算        |
| 预算控制   | 单据/凭证保存时检查，可设置禁止/预警/审批三种控制级别 |
| 预算执行分析 | 实际 vs 预算对比，计算执行率与差异          |
| 预算调整   | 追加调减预算，走审批流，保留调整历史           |

### 4.10 报表中心

提供法定财务报表及自定义报表。

| 子功能   | 说明                            |
| ----- | ----------------------------- |
| 资产负债表 | 根据科目余额及重分类规则自动生成              |
| 利润表   | 当月及累计发生额计算                    |
| 现金流量表 | 通过凭证分录指定或自动匹配现金流量项目           |
| 科目余额表 | 按期间、层级展示期初/本期发生/期末余额          |
| 自定义报表 | 拖拽式设计器，保存为模板                  |
| 导出    | Excel/PDF，EasyExcel 支持百万行流式导出 |

**性能优化**：使用 PostgreSQL 物化视图预计算报表数据，支持定期刷新。

### 4.11 财务分析模块

| 子功能  | 说明                                  |
| ---- | ----------------------------------- |
| 财务指标 | 毛利率、净利率、流动比率、速动比率、资产负债率、ROE 等，公式可配置 |
| 杜邦分析 | 净资产收益率分解树状图                         |
| 趋势分析 | 收入/成本/费用多期趋势，同比环比                   |
| 预警设置 | 指标超阈值自动消息通知                         |

**AI 结合点**：智能分析助手，支持自然语言查询（如“本月销售费用增长原因”），AI 解析意图并返回图表或分析。

### 4.12 AI 辅助模块（详细设计）

AI 模块为独立 Python 微服务，通过 RabbitMQ 与 Java 主系统异步通信，所有 AI 结果存入 PostgreSQL（`t_ai_task` 及相关表的 `ai_*`、`ai_confidence` 等字段），需人工审核方可生效。

> **规则引擎与 AI 服务的分层定位**：
>
> 系统采用"规则引擎 + AI 语义 + 兜底启发式"三层分类架构，确保无 AI 服务时系统仍能正常运行：
>
> ```
> 优先级:  规则引擎(高) > AI 语义(中) > 兜底启发式(低)
>              ↓              ↓              ↓
>           确定性高         灵活性强      100%有结果
>           Java 本地       Python 微服务   Java 本地
> ```
>
> - **规则引擎**（Java 本地）：银行手续费、利息、税费等高频确定性场景，关键词/正则匹配，毫秒级响应
> - **AI 语义**（Python 微服务）：陌生对手方、模糊摘要理解、费用类型推荐，需人工审核确认
> - **兜底启发式**（Java 本地）：硬编码关键词分组按优先级匹配，确保每笔流水有分类

#### 4.12.1 数据文件导入智能识别

系统在导入银行流水、发票、费用单据等数据文件时，自动进行智能识别与分类，大幅减少人工录入工作量。

**4.12.1.1 银行流水导入智能分类**

银行流水上传后，系统依次执行三层分类引擎，确保每条流水均有分类：

```
阶段一：规则引擎匹配（8 条种子规则）
  └─ 按优先级逐条检查方向→关键词/正则→第一命中即停
  └─ 输出：8 种标准分类（bank_fee/interest_income/business_receipt/...）
  └─ 并记录命中规则 ID，用于后续统计与审计

阶段二：兜底启发式分类（关键词分组）
  └─ 规则引擎未命中时执行
  └─ 按优先级逐组检查：银行费用→利息→保险→税务→社保→工资→内部转账→收款→付款
  └─ 每组硬编码关键词，如手续费/利息/货款/增值税...
  └─ 确保 100% 流水有分类

阶段三：AI 语义增强（可选，需启用 AI 服务）
  └─ 规则引擎+兜底置信度均 <60% 时触发
  └─ 调用 Python 匹配服务，对摘要文本做语义嵌入
  └─ 与历史分类模板计算余弦相似度
  └─ 返回建议分类 + 置信度评分
  └─ 结果写入 ai_suggested_action / ai_confidence / ai_business_scene
```

分类结果输出到流水记录后，前端以彩色标签展示：

| 置信度范围 | 颜色 | 操作建议 |
|:---|:---:|:---|
| ≥80% | 🟢 绿色 | 一键确认采纳 |
| 60-79% | 🟡 黄色 | 重点核对后再确认 |
| <60% | ⚪ 灰色 | 建议人工判断，不自动推荐 |

> **场景案例**：出纳上传网银导出的对账单 Excel 后，系统自动识别摘要"网银转账手续费 15 元"→ 命中规则引擎"银行手续费"规则 → 标签显示"银行费用 🟢（规则命中）"。出纳在工作台批量勾选确认，系统自动生成银行费用凭证。

**4.12.1.2 发票导入智能客户/供应商识别**

销售发票/采购发票导入时，系统自动根据发票中的购方/销方信息匹配已有客商档案：

```
读取发票行数据（发票号、金额、日期、购方/销方名称、税号）
  ↓
客户/供应商自动匹配（两阶段）
  ├─ 阶段一：按税号精确匹配（buyerTaxId / sellerTaxId）
  │   └─ 匹配成功 → 直接关联客商ID
  ├─ 阶段二：按名称模糊匹配（pg_trgm 相似度 ≥ 0.8）
  │   └─ 匹配成功 → 提示"疑似已有客商"供确认
  └─ 阶段三：自动创建新客商档案
      └─ 匹配失败 → 根据发票信息自动创建客户/供应商记录
      └─ 创建后标记来源为"发票导入自动创建"
  ↓
发票写入数据库，关联正确的客商 ID
```

**红冲发票自动关联**：导入红字发票时，系统自动在备注/源发票号字段中提取原蓝字发票号码，匹配系统中已存在的蓝字发票，将其状态标记为 `REVERSED`，并生成红字应收/应付记录。

> **场景案例**：会计导入税务局导出的销项发票 Excel，其中包含客户"北京华联商贸有限公司（税号 91110108...）"。系统在客商库中按税号匹配到已有的客户档案，自动关联。另有一张红冲发票，备注列写明"被红冲蓝字发票号码：12345678"，系统自动找到对应的蓝字发票并标记已红冲。

**4.12.1.3 费用发票 OCR 智能识别与科目推荐**

员工上传费用发票图片时：

1. 上传至 MinIO，前端发起 OCR 请求
2. Java 创建 `t_ai_task`，通过 RabbitMQ 投递至 Python OCR 服务
3. OCR 提取：发票号码、金额、税额、开票日期、商户名称
4. **AI 费用归类**：根据商户名称+商品名自动推荐费用类型
   - 如"美团" → "业务招待费"（置信度 95%）
   - 如"中国石化" → "车辆费"（置信度 92%）
   - 如"携程旅行" → "差旅费"（置信度 90%）
5. **AI 科目推荐**：根据费用类型自动推荐借方科目
   - 业务招待费 → 6602.03 管理费用-业务招待费
   - 车辆费 → 6602.04 管理费用-车辆使用费
   - 差旅费 → 6602.05 管理费用-差旅费
6. 前端展示识别结果 + 推荐费用类型 + 置信度，用户可采纳或手动修改
7. 用户确认后自动填充报销单字段，生成费用报销单

#### 4.12.2 业务单据与凭证自动生成

智能分类完成后，系统根据分类结果自动触发后续处理，减少从流水到凭证的人工环节。

**4.12.2.1 银行流水→业务单据自动创建**

| 流水分类 | 自动动作 | 生成单据类型 | 关键说明 |
|:---|:---|:---:|:---|
| `business_receipt`（业务收款）| 自动创建收款单 | 收款单 | 关联对方户名匹配的客户，金额自动填入 |
| `business_payment`（业务付款）| 自动创建付款单 | 付款单 | 关联对方户名匹配的供应商，金额自动填入 |
| `internal_transfer`（内部转账）| 自动创建银行转账单 | 银行转账单 | 对方账户从流水识别，不涉及损益 |

自动创建的单据状态为`草稿（DRAFT）`，待出纳补充信息后提交审批。出纳可在列表页查看所有由流水自动生成的单据（标记来源为"FROM_BANK_TXN"）。

> **场景案例**：银行流水摘要"深圳华强电子-货款-¥50,000"被分类为 `business_receipt`。系统自动创建一张收款单，对方户名自动关联客商"深圳华强电子"，金额 50,000 元。出纳进入单据详情页，只需勾选待核销的销售发票即可完成核销。

**4.12.2.2 银行流水→凭证自动生成**

对于不需要经过业务单据的简单业务类型，系统可直接生成凭证草稿：

| 流水分类 | 凭证模板 | 分录 | 说明 |
|:---|:---|:---|:---|
| `bank_fee`（银行费用）| 银行费用凭证 | 借：**6602.01 财务费用-手续费** | 金额取流水金额 |
| | | 贷：**1002 银行存款** | 账户取流水银行账户 |
| `interest_income`（利息收入）| 利息收入凭证 | 借：**1002 银行存款** | 金额取流水金额 |
| | | 贷：**6602.02 财务费用-利息收入（红字）** | 或贷方负数 |
| `tax_payment`（税务缴费）| 缴税凭证 | 借：**2221 应交税费-各明细** | 若摘要含具体税种则自动匹配 |
| | | 贷：**1002 银行存款** | 金额取流水金额 |
| `social_security`（社保缴费）| 社保缴费凭证 | 借：**2211 应付职工薪酬-社保** | |
| | | 贷：**1002 银行存款** | |
| `insurance_fee`（保险费用）| 保险费用凭证 | 借：**6602.06 管理费用-保险费** | |
| | | 贷：**1002 银行存款** | |

生成的凭证状态为`草稿（DRAFT）`，进入总账待审核列表。出纳批量确认时，系统自动完成"确认流水→生成凭证"全流程。

凭证模板可通过 `t_voucher_template` 表由财务经理配置，支持按银行账户、金额范围等维度设定不同的科目映射规则。

> **场景案例**：一笔银行流水摘要"代扣账户管理费 300 元"，分类为 `bank_fee`。系统直接生成一张草稿凭证：借 6602.01 财务费用-手续费 300 元 / 贷 1002 银行存款 300 元。会计在凭证审核工作台查看并确认。

**4.12.2.3 发票→凭证自动生成**

根据发票类型自动生成标准凭证：

| 发票类型 | 凭证模板 | 分录 |
|:---|:---|:---|
| 销售发票（确认收入） | 销售收入凭证 | 借：**1122 应收账款**（价税合计）/ 贷：**5001 主营业务收入**（不含税）/ 贷：**2221.01 应交税费-销项税** |
| 进项发票（采购入库） | 采购入库凭证 | 借：**1403 原材料/库存商品** / 借：**2221.02 应交税费-进项税** / 贷：**2202 应付账款** |

发票确认后自动触发凭证生成，与单据管理模块的 `generateVoucher` 流程一致，状态流转：`发票已确认 → 自动生成凭证草稿 → 总账审核 → 过账`。

#### 4.12.3 智能辅助核销

核销是连接业务流（发票）与资金流（收付款单/流水）的关键环节。系统提供多层次的智能辅助核销，降低人工匹配工作量。

**4.12.3.1 收款核销推荐**

创建或确认收款单时，系统自动推荐待核销的销售发票：

```
出纳确认收款单（来源：银行流水自动创建 或 手工录入）
  ↓
系统自动推荐待核销发票
  ├─ 按客户筛选：取出该客户所有已确认未核销的销售发票
  ├─ 按金额匹配：优先推荐金额一致的发票
  ├─ 按摘要相似度：与收款单摘要含相同关键词的发票优先
  └─ 按到期日排序：将已逾期或临近到期发票置顶
  ↓
前端以列表展示推荐结果：
  ├─ 每张发票的：发票号/日期/原金额/未核销余额/匹配度评分
  ├─ 匹配度评分 = 金额一致(0.4) + 摘要相似度(0.4) + 对方户名匹配(0.2)
  ├─ 完全匹配项高亮绿色，可直接一键核销
  └─ 部分匹配项显示黄色，提示金额差异
  ↓
出纳确认核销方案 → 执行核销 → 发票状态更新为部分核销/已核销
```

**4.12.3.2 付款核销推荐**

同理，创建付款单时自动推荐待核销的采购发票，匹配策略与收款核销对称：

- 按供应商筛选
- 金额一致+摘要相似度+对方户名匹配
- 推荐结果分级展示

**4.12.3.3 银行流水自动匹配回款核销**

对于银行流水分类为 `business_receipt` 或 `business_payment` 的流水，系统除自动创建收/付款单外，还会尝试直接匹配对应的未核销发票：

```
银行流水 → 分类为 business_receipt
  ↓
自动创建收款单
  ↓
自动查找客户（按对方户名匹配客商档案）
  ↓
按客户查找所有状态为已确认/部分核销的销售发票
  ↓
执行自动核销匹配算法：
  ├─ 金额精确匹配（0.4）: 流水金额 = 发票未核销余额
  ├─ 摘要语义匹配（0.4）: 流水摘要中包含发票号码或发票中包含流水摘要关键词
  └─ 对方户名匹配（0.2）: 流水对方户名 = 发票客户名称
  ↓
匹配度 ≥ 0.7 的候选发票展示给出纳
  ↓
出纳确认 → 执行核销 → 发票核销完成 → 系统自动生成收款凭证
```

**核销记录追溯**：每笔核销操作记录在 `t_reconciliation_log` 表中，可从发票详情页的"核销记录"选项卡追溯至收/付款单和银行流水，形成"发票→收款单→银行流水"的完整资金链路。

> **场景案例**：出纳导入的银行流水"摘要：回款-发票号 INV-2026-0058-客户：北京华联-¥30,000"被分类为 `business_receipt`。系统自动创建收款单，按客户名称匹配到"北京华联"，找到该客户未核销的销售发票 INV-2026-0058（金额 30,000 元，未核销余额 30,000 元）。系统推荐核销（匹配度 1.0），出纳一键确认，核销完成并自动生成收款凭证。

#### 4.12.4 异常检测

- 定时任务或凭证记账后触发，将近期凭证数据发送至异常检测模型。
- 模型检查借贷不平衡、科目使用异常、金额突增、摘要与科目不匹配等风险。
- 结果回写凭证的 `ai_tags` 和 `ai_review_score`，前端高亮提示风险凭证。
- 异常等级：LOW（提醒）/ MEDIUM（黄色警告）/ HIGH（建议退回）/ CRITICAL（阻断过账）。

| 检测项 | 说明 | 默认严重度 |
|:---|:---|:---:|
| 借贷不平衡 | 借方总额≠贷方总额 | CRITICAL |
| 金额异常 | 单笔分录金额 > 1,000,000 元 | MEDIUM |
| 摘要可疑 | 含"补亏/退回/调整/调账/暂估"等关键词 | LOW |
| 科目串户 | 典型科目搭配违反常规（如借固定资产/贷应付账款但应为银行存款）| MEDIUM |
| 期间错误 | 凭证期间与当前开放期间不符 | HIGH |

#### 4.12.5 智能问答

- 将财务制度文档、科目体系等存入 PostgreSQL，利用 `tsvector` 和 `pg_trgm` 建立全文索引。
- 用户自然语言提问，AI 服务理解意图并转换为 SQL 或关键词检索，返回结果或分析图表。
- 向量检索可用于 FAQ 匹配。

#### 4.12.6 趋势预测

- 汇总历史财务数据（收入/费用），通过时间序列预测模型预测未来趋势，结果存入分析报表，前端图表展示。

***

## 五、PostgreSQL 数据库设计（核心表）

### 5.1 选择 PostgreSQL 的优势

- **精确数值**：`NUMERIC(18,2)` 保证财务计算精度。
- **JSONB**：存储 OCR 数据、辅助核算、AI 任务负载等半结构化数据，灵活扩展。
- **全文搜索**：`tsvector` + `pg_trgm` 实现高效文本搜索和模糊匹配。
- **pgvector**：原生支持向量存储和相似度检索，支撑智能对账、问答。
- **物化视图**：预计算报表数据，极大提升查询性能。
- **序列**：使用 `IDENTITY` 列或 `SEQUENCE`，比 MySQL 自增更标准。
- **逻辑复制与事件触发器**：可用于 AI 近实时感知数据变化。

### 5.2 建表语句示例

```sql
-- 启用扩展
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;

-- 科目表
CREATE TABLE t_subject (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    code VARCHAR(32) NOT NULL,
    name VARCHAR(64) NOT NULL,
    parent_id BIGINT REFERENCES t_subject(id),
    level INT,
    direction CHAR(1) CHECK (direction IN ('D','C')),
    is_cash BOOLEAN DEFAULT FALSE,
    aux_item_type VARCHAR(32), -- 辅助核算类型，如 DEPT/CUSTOMER/SUPPLIER/PROJECT
    UNIQUE(code)
);

-- 凭证主表
CREATE TABLE t_voucher (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    voucher_no VARCHAR(32) NOT NULL,
    period VARCHAR(6) NOT NULL,
    voucher_type VARCHAR(16),
    status VARCHAR(16) DEFAULT 'DRAFT',
    total_debit NUMERIC(18,2),
    total_credit NUMERIC(18,2),
    summary TEXT,
    created_by BIGINT,
    created_at TIMESTAMPTZ DEFAULT now(),
    posted_at TIMESTAMPTZ,
    audit_at TIMESTAMPTZ,
    reversed_from BIGINT,
    ai_tags TEXT[],
    ai_review_score FLOAT,
    detail_json JSONB,
    UNIQUE(period, voucher_no)
);

-- 凭证分录表
CREATE TABLE t_voucher_entry (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    voucher_id BIGINT NOT NULL REFERENCES t_voucher(id),
    subject_id BIGINT NOT NULL,
    debit NUMERIC(18,2) DEFAULT 0,
    credit NUMERIC(18,2) DEFAULT 0,
    summary TEXT,
    assist_json JSONB,
    embedding VECTOR(768) -- 文本嵌入向量
);
CREATE INDEX idx_entry_summary_vec ON t_voucher_entry USING ivfflat (embedding vector_cosine_ops);

-- 科目余额表
CREATE TABLE t_subject_balance (
    subject_id BIGINT NOT NULL,
    period VARCHAR(6) NOT NULL,
    begin_balance NUMERIC(18,2),
    debit_total NUMERIC(18,2),
    credit_total NUMERIC(18,2),
    end_balance NUMERIC(18,2),
    PRIMARY KEY (subject_id, period)
);

-- 业务单据表
CREATE TABLE t_business_doc (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    doc_no VARCHAR(32) NOT NULL,
    doc_type VARCHAR(32) NOT NULL,
    doc_date DATE NOT NULL,
    amount NUMERIC(18,2),
    status VARCHAR(16) DEFAULT 'DRAFT',
    supplier_id BIGINT,
    customer_id BIGINT,
    applicant_id BIGINT,
    dept_id BIGINT,
    summary TEXT,
    ocr_data JSONB,
    embedding VECTOR(768),
    voucher_id BIGINT,
    created_at TIMESTAMPTZ DEFAULT now(),
    UNIQUE(doc_type, doc_no)
);

-- AI 任务表
CREATE TABLE t_ai_task (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    task_type VARCHAR(32),
    source_id BIGINT,
    status VARCHAR(16) DEFAULT 'PENDING',
    request_payload JSONB,
    result_payload JSONB,
    model_version VARCHAR(32),
    created_at TIMESTAMPTZ DEFAULT now(),
    completed_at TIMESTAMPTZ
);

-- 分类规则表（银行流水智能分类引擎）
CREATE TABLE t_classification_rule (
    id                 BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    tenant_id          BIGINT NOT NULL,
    name               VARCHAR(100) NOT NULL,
    rule_type          VARCHAR(30) NOT NULL DEFAULT 'keyword',        -- keyword / keyword_regex / counterparty_match
    pattern            TEXT NOT NULL,
    match_field        VARCHAR(30) NOT NULL DEFAULT 'description',    -- description / counterparty
    direction          VARCHAR(10),                                   -- in / out / 空(不限)
    classification     VARCHAR(50) NOT NULL,                          -- bank_fee / interest_income / ...
    priority           INT NOT NULL DEFAULT 0,
    is_active          BOOLEAN NOT NULL DEFAULT TRUE,
    debit_subject_id   BIGINT,                                        -- 借方科目（可选）
    credit_subject_id  BIGINT,                                        -- 贷方科目（可选）
    created_by         BIGINT,
    updated_by         BIGINT,
    created_at         TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at         TIMESTAMP NOT NULL DEFAULT NOW(),
    deleted            INT DEFAULT 0
);
CREATE INDEX idx_cls_rule_tenant ON t_classification_rule(tenant_id, is_active, priority);

-- AI 反馈日志表（记录人工修正，用于模型学习）
CREATE TABLE t_ai_feedback_log (
    id                   BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    tenant_id            BIGINT NOT NULL,
    bank_txn_id          BIGINT NOT NULL,
    ai_suggested_action  VARCHAR(50),
    ai_confidence        INT,
    ai_business_scene    VARCHAR(100),
    human_action         VARCHAR(50) NOT NULL,
    human_modified_fields JSONB,
    created_by           BIGINT,
    created_at           TIMESTAMP NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_ai_feedback_txn ON t_ai_feedback_log(bank_txn_id);
CREATE INDEX idx_ai_feedback_tenant ON t_ai_feedback_log(tenant_id);
```

其他表（如银行账户、日记账、固定资产卡片、往来明细、核销记录、税务相关表等）均采用 `NUMERIC` 精确金额，并适当使用 `JSONB` 存储扩展信息。

### 5.3 物化视图加速报表

```sql
CREATE MATERIALIZED VIEW mv_balance_snapshot_202606 AS
SELECT subject_id, period, SUM(debit_total) ... 
FROM t_subject_balance WHERE period <= '202606' GROUP BY subject_id, period;
```

通过定时任务或事件触发刷新。

***

## 六、AI 服务集成架构

### 6.1 通信机制

- **主流程（Java）** → 创建 AI 任务 → 发送 RabbitMQ → **AI 服务（Python）** 消费处理。
- AI 服务将结果通过回调接口（REST）或响应队列返回 Java 主系统，更新 `t_ai_task` 及业务表。

### 6.2 AI 服务组件

| 服务     | 技术栈                            | 功能            |
| ------ | ------------------------------ | ------------- |
| OCR 服务 | PaddleOCR / Tesseract + OpenCV | 发票、回单图片识别     |
| 文本嵌入服务 | text2vec-base-chinese / BGE    | 生成摘要文本的向量     |
| 异常检测   | Isolation Forest / LSTM        | 识别凭证异常        |
| 预测服务   | Prophet / ARIMA                | 收入/费用趋势预测     |
| 问答服务   | LangChain + LLM (本地部署或 API)    | 财务制度问答、自然语言查询 |

### 6.3 人工审核闭环

所有 AI 自动生成或匹配的结果必须经过“建议→人工确认→生效”的流程，确保财务数据的严谨性。例如：

- 自动生成的凭证分录高亮显示，需会计人员确认后记账。
- 银行对账匹配建议需出纳逐一确认。
- 异常检测结果仅作为预警，不自动修改数据。

***

## 七、关键设计模式与保障

- **事务一致性**：所有涉及会计凭证的写入操作均使用 `@Transactional`，保证原子性。
- **防篡改**：已审核/已记账凭证不可修改，只能红冲；单据生成凭证后锁定。
- **分布式序列号**：Redis INCR + DB 唯一索引，保证凭证号、单据号全局唯一。
- **数据权限**：MyBatis 拦截器根据用户部门动态注入 SQL，实现数据隔离。
- **状态机**：所有核心实体均定义严格状态流转，避免非法状态。
- **审计日志**：AOP 记录操作前后数据快照，存储于 `t_audit_log` 的 `jsonb` 字段。
- **并发控制**：科目余额更新使用乐观锁或 `SELECT ... FOR UPDATE`。

***

## 八、部署与运维

| 环境         | 配置                         | 说明                     |
| ---------- | -------------------------- | ---------------------- |
| 开发         | 单机 Docker                  | 所有组件单实例                |
| 测试         | 2核4G × 2                   | 模拟真实数据，集成测试            |
| 生产         | 4核8G × 2+                  | Nginx 负载均衡，后端无状态可水平扩展  |
| PostgreSQL | Patroni + etcd 高可用         | 一主一从自动切换，连接池 PgBouncer |
| Redis      | 哨兵模式                       | 高可用缓存                  |
| MinIO      | 分布式集群                      | 附件高可用                  |
| AI 服务      | 独立部署，GPU 可选                | 推理服务可弹性伸缩              |
| 监控         | Prometheus + Grafana + ELK | 全方位监控与日志检索             |

***

## 九、总结

本方案基于 **PostgreSQL + Java + AI** 构建了一个完整、稳健、智能的财务软件系统。借助 PostgreSQL 的现代特性（向量、JSONB、物化视图），我们将 AI 能力有机融入财务流程，实现了从业务单据到总账报表的全链条智能化。系统设计兼顾了财务系统的严谨性（事务、审计、防篡改）与现代软件架构的灵活性（分层、异步、容器化），可直接作为产品研发的详细蓝图。
